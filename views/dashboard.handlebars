<div class="mb-5 pt-3">
    <h2 class="col-12 text-center">Dashboard | My Post</h2><hr>
    {{#if posts.length}}
</div>

{{#each posts as | post|}}
    {{> post-info post}}
{{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">You currently have no post(s)</h6>

{{/if}}


